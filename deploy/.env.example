# ==================================================
# Vault Crypto Plugin - Production Configuration
# ==================================================
# Usage: cp .env.example .env && vim .env

# ---------- Vault Basic ----------
VAULT_VERSION=1.21
VAULT_CONTAINER_NAME=vault-crypto-prod
VAULT_PORT=8200
VAULT_CLUSTER_PORT=8201
VAULT_DATA_VOLUME=vault-crypto-data

# ---------- Vault FQDN ----------
# Fully qualified domain name or IP address for Vault
# Used in api_addr and cluster_addr in HCL config
VAULT_FQDN=vault.example.com

# ---------- TLS ----------
# Set to true to disable TLS (only when using a reverse proxy for TLS termination)
TLS_DISABLE=false

# ---------- Unseal Method ----------
# Options: shamir, awskms
UNSEAL_METHOD=shamir

# ---------- AWS KMS (only for awskms unseal method) ----------
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
# AWS_KMS_KEY_ID=

# ---------- Plugin ----------
PLUGIN_VERSION=v0.1.0
PLUGIN_NAME=vault-plugin-crypto
PLUGIN_MOUNT_PATH=crypto

# ---------- Vault Init (first time only) ----------
KEY_SHARES=5
KEY_THRESHOLD=3

# ---------- Logging ----------
# Options: trace, debug, info, warn, error
LOG_LEVEL=info
